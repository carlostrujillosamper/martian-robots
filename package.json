{
  "name": "martian-robots",
  "version": "0.1.0",
  "scripts": {
    "babel": "babel src --out-dir dist/",
    "test": "npm run babel && mocha test/**"
  },
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.4.0",
    "chai": "^3.5.0",
    "mocha": "^3.3.0"
  }
}
